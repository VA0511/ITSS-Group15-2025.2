# API Backend URL — Go server chạy ở port 8080
VITE_API_URL=http://localhost:8080

# Đặt thành 'true' nếu muốn dùng Mock API (khi backend chưa chạy)
# Đặt thành 'false' hoặc xoá dòng này để kết nối backend thật
VITE_USE_MOCK_AUTH=false
